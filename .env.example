# =============================================================================
# QDash Environment Configuration
# =============================================================================
# Copy this file to .env and customize as needed.
#
# Most settings have sensible defaults in compose.yaml.
# Only uncomment and set values if you need to override them.
#
# For volume path customization, prefer using compose.override.yaml
# See example.compose.override.yaml for examples.
# =============================================================================

# Environment name (used as Docker Compose project name)
ENV="oqtopus"

# =============================================================================
# Required: Database Credentials
# =============================================================================
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=example
MONGO_DB_NAME=qdash

POSTGRES_USER=postgres
POSTGRES_PASSWORD=prefect
POSTGRES_DB=prefect

# Auto-creates admin user on first startup if set
QDASH_ADMIN_USERNAME="admin"
QDASH_ADMIN_PASSWORD="passw0rd"

# =============================================================================
# Required: Ports (defaults shown)
# =============================================================================
MONGO_PORT=27017
MONGO_EXPRESS_PORT=8081
POSTGRES_PORT=5432
PREFECT_PORT=4200
API_PORT=5715
UI_PORT=5714

# =============================================================================
# Required: Volume Paths
# =============================================================================
# Prefer using compose.override.yaml for path customization.
# These have defaults in compose.yaml, only set if needed.
#
POSTGRES_DATA_PATH="./postgres_data"
MONGO_DATA_PATH="./mongo_data/data/db"
CALIB_DATA_PATH="./calib_data"
CONFIG_PATH="./config/qubex"
CALIBTASKS_PATH="./src/qdash/workflow/calibtasks"

# =============================================================================
# Required: Backend Configuration
# =============================================================================
# Default backend for calibration workflows
# Options: "qubex" (real hardware), "fake" (testing without hardware)
DEFAULT_BACKEND="fake"

# =============================================================================
# Optional: API Configuration
# =============================================================================
# PREFECT_API_URL="http://prefect:4200/api"
# CLIENT_URL=""



# =============================================================================
# Optional: Integrations
# =============================================================================
# Slack notifications
# SLACK_BOT_TOKEN=""
# SLACK_CHANNEL_ID=""
# SLACK_APP_TOKEN=""

# GitHub integration
# CONFIG_REPO_URL=""
# GITHUB_TOKEN=""
# GITHUB_USER=""

# Cloudflare Tunnel (use 'docker compose --profile tunnel up')
# TUNNEL_TOKEN=""

# Cloudflare Access
# CF_ACCESS_CLIENT_ID=""
# CF_ACCESS_CLIENT_SECRET=""
# CF_ACCESS_DOMAIN=""
# QDASH_API_TOKEN=""

# =============================================================================
# Optional: AI Copilot (Ollama)
# =============================================================================
# Enable the AI chat assistant in the UI
# NEXT_PUBLIC_COPILOT_ENABLED="true"
# OLLAMA_MODEL="gpt-oss:20b"
# OLLAMA_URL="http://host.docker.internal:11434"


# =============================================================================
# Advanced: Network Configuration
# =============================================================================
# For custom network settings, use compose.override.yaml instead.
# SUBNET=""
# GATEWAY=""
